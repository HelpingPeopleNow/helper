[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    slow: slow tests that take >1s
    integration: integration tests that bind a real gRPC server
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestUnhandledThreadExceptionWarning
